---
extends: default

ignore: |
    .github/
    galaxy_roles/
    inventories/sugardon01/group_vars/

rules:
  # 120 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 120
    level: warning
